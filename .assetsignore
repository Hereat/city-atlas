# 不当成静态资源发布的东西。
# 没有这份，wrangler 会把 .git 整个目录当成资源上传（2026-09-07 实测多出一千多个文件），
# 既浪费 Workers 的文件配额，也让仓库内部对象出现在公网路径上。
.git
.assetsignore
.wrangler

# 部署自身的东西不是数据。`pipeline/` 相反——那是 ODbL 要求随数据提供的脚本副本，
# 必须发出去，所以不在这张单子上。
wrangler.jsonc
worker.js
